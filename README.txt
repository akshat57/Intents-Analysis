Use collect_data.py to use the audio files to generate phones.
It also reads text from the audio_info files and combines it with the phone data and stores it in data.pickle
It requires the directory 'Data' to be present in the previous folder. It can also be adjusted in the code


